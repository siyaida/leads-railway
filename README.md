# Siyada Lead Generation — Railway Deployment

Full-stack lead generation app (React + FastAPI + PostgreSQL) packaged for one-click Railway deployment.

- **Backend**: FastAPI serving `/api/*` endpoints
- **Frontend**: Pre-built React SPA served as static files
- **Database**: PostgreSQL (provisioned via Railway)

## Quick Deploy

1. Connect this repo to Railway
2. Add a PostgreSQL plugin
3. Set the required environment variables (see GUIDE.md)
4. Deploy
